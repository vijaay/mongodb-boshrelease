{
  "_id" : "<%= p("replication.replSetName") %>",
  "version" : 1,
  "members" : [
    <% p("replication.servers").each_with_index do |ip, index| %>
    {
      "_id" : #{index},
      "host" : "#{ip}:<%= p("net.port") %>"
    },
    <% end %>
  ]
}
