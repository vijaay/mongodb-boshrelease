---
meta:
  environment: NAME
  dns_root: microbosh
  network_name: mongodb1
  security_groups:
    - mongodb

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: mongodb
    version: latest
